"""
The five olfactory benchmarks, rebuilt to measure what their cited papers
measured.

Each module here replaces one benchmark in ``scripts/run_all_validations.py``.
Every one carries, in its own docstring, the quotation from the source paper
that its criterion is derived from, and every result file it writes carries a
``target_corrections`` block naming any target that was changed and why.

See ``docs/03_validation/BENCHMARK_VALIDITY_AUDIT.md`` for the full audit.
"""
