# Wave-Based Probabilistic Neural Simulation
# Requirements file for Python dependencies
# Python version: 3.9 or higher

# =============================================================================
# CORE DEPENDENCIES (Required)
# =============================================================================

# Numerical computation
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0

# =============================================================================
# GPU ACCELERATION (Choose based on hardware)
# =============================================================================

# Apple Silicon (M-series chips) - RECOMMENDED for Mac
mlx>=0.10.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# NVIDIA CUDA - for NVIDIA GPUs
# torch>=2.0.0; sys_platform == 'linux'

# AMD ROCm - for AMD GPUs
# torch>=2.0.0+rocm5.4.2; sys_platform == 'linux'

# Note: CPU-only mode works with just NumPy (no GPU packages needed)

# =============================================================================
# VISUALIZATION (Required for figures)
# =============================================================================

matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# =============================================================================
# DATA HANDLING
# =============================================================================

pandas>=1.5.0,<3.0.0
h5py>=3.8.0,<4.0.0  # For large data files
tables>=3.8.0,<4.0.0  # PyTables for HDF5

# =============================================================================
# CONNECTOME DATA
# =============================================================================

# Network analysis
networkx>=3.0,<4.0

# Graph operations
igraph>=0.10.0,<1.0.0; python_version >= '3.9'

# =============================================================================
# OPTIMIZATION & SCIENTIFIC COMPUTING
# =============================================================================

# Optimization algorithms
scikit-learn>=1.2.0,<2.0.0

# Statistical analysis
statsmodels>=0.14.0,<1.0.0

# =============================================================================
# JUPYTER NOTEBOOK SUPPORT (Optional)
# =============================================================================

jupyter>=1.0.0,<2.0.0
ipykernel>=6.20.0,<7.0.0
ipywidgets>=8.0.0,<9.0.0

# =============================================================================
# DEVELOPMENT & TESTING (Optional)
# =============================================================================

# Testing
pytest>=7.2.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# Code quality
black>=23.0.0,<24.0.0  # Code formatter
flake8>=6.0.0,<7.0.0  # Linter
mypy>=1.0.0,<2.0.0  # Type checking

# =============================================================================
# DOCUMENTATION (Optional)
# =============================================================================

sphinx>=5.3.0,<6.0.0
sphinx-rtd-theme>=1.2.0,<2.0.0

# =============================================================================
# UTILITIES
# =============================================================================

# Progress bars
tqdm>=4.64.0,<5.0.0

# Configuration management
pyyaml>=6.0,<7.0
toml>=0.10.0,<1.0.0

# File operations
pathlib>=1.0.1  # Usually included in Python 3.4+

# =============================================================================
# DATA ACQUISITION
# =============================================================================

# HTTP requests for downloading data
requests>=2.28.0,<3.0.0
urllib3>=1.26.0,<2.0.0

# =============================================================================
# PLATFORM-SPECIFIC NOTES
# =============================================================================

# macOS (Apple Silicon):
#   - Install MLX for GPU acceleration
#   - pip install -r requirements.txt
#
# Linux (NVIDIA GPU):
#   - Uncomment torch with CUDA support above
#   - pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#
# Linux (AMD GPU):
#   - Uncomment torch with ROCm support above
#   - Follow PyTorch ROCm installation guide
#
# Windows:
#   - CUDA support via PyTorch
#   - pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#
# CPU-only (any platform):
#   - No GPU packages needed
#   - Will automatically fall back to NumPy

# =============================================================================
# VERSION COMPATIBILITY
# =============================================================================

# Tested on:
# - Python 3.9, 3.10, 3.11
# - macOS 14.0+ (Sonoma)
# - Ubuntu 22.04 LTS
# - Windows 11

# Known issues:
# - Python 3.12: MLX support experimental
# - Python <3.9: Not supported (type hints, pattern matching)

# =============================================================================
# MINIMAL INSTALLATION
# =============================================================================

# For basic functionality (no GPU, no figures):
#   numpy>=1.24.0
#   scipy>=1.10.0
#
# This is sufficient for running simulations but not for:
#   - GPU acceleration
#   - Generating publication figures
#   - Jupyter notebooks

# =============================================================================
# INSTALLATION COMMANDS
# =============================================================================

# Basic installation:
#   pip install -r requirements.txt

# Development installation:
#   pip install -r requirements.txt
#   pip install -e .  # Editable install

# Specific GPU backend:
#   # Apple Silicon
#   pip install -r requirements.txt
#   
#   # NVIDIA CUDA
#   pip install -r requirements.txt
#   pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
#   
#   # AMD ROCm
#   pip install -r requirements.txt
#   pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/rocm5.4.2

# =============================================================================
# PACKAGE SIZES (Approximate)
# =============================================================================

# numpy: ~20 MB
# scipy: ~40 MB
# matplotlib: ~50 MB
# mlx: ~100 MB (Apple Silicon only)
# torch: ~2 GB (with CUDA), ~100 MB (CPU-only)
#
# Total minimal install: ~110 MB
# Total with MLX: ~210 MB
# Total with PyTorch CUDA: ~2.2 GB

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# If you encounter SSL/certificate errors:
#   pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org <package>

# If you encounter version conflicts:
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt --upgrade

# For ARM64 (Apple Silicon) compatibility issues:
#   Install Rosetta 2 if needed: softwareupdate --install-rosetta
#   Use native ARM64 Python: python3 --version should show arm64

# Memory requirements:
#   Minimal: 4 GB RAM
#   Recommended: 16 GB RAM
#   Optimal: 32+ GB RAM (for full brain simulations)
