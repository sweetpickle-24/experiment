# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
PyWavelets>=1.4.0
pyyaml>=6.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# GPU acceleration (CUDA 12.x) - OPTIONAL but HIGHLY RECOMMENDED
# Install ONLY ONE of these based on your CUDA version:
#
# For CUDA 12.x (most common):
# cupy-cuda12x>=12.0.0
#
# For CUDA 11.x:
# cupy-cuda11x>=12.0.0
#
# For ROCm (AMD GPUs):
# cupy-rocm-5-0>=12.0.0
#
# Installation instructions:
# 
# 1. Check your CUDA version:
#    nvidia-smi
#
# 2. Install CuPy matching your CUDA version:
#    pip install cupy-cuda12x   # for CUDA 12.x
#    pip install cupy-cuda11x   # for CUDA 11.x
#
# 3. Verify GPU is working:
#    python -c "import cupy as cp; print(f'GPU count: {cp.cuda.runtime.getDeviceCount()}')"
#
# NOTE: The system will automatically fall back to CPU if CuPy is not installed.
#       GPU acceleration provides 10-100x speedup for coupling force computation.
