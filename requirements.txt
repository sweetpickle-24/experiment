# Dependencies
#
# Last verified against actual imports: 2026-09-03, by AST-walking every .py under
# hive/, scripts/, tests/, benchmarks/, plus server.py and validation_utils.py.
#
# The previous version of this file listed 76 packages, most of which nothing in the
# repository imported (jupyter, sphinx, black, flake8, mypy, networkx, igraph,
# statsmodels, h5py, tables, seaborn, requests, and `pathlib`, which has been part of
# the standard library since Python 3.4). It also omitted fastapi, uvicorn, pydantic
# and opencv, which are genuinely required.

# ── Core: required to run the engine and the validation suite ────────────────
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0
scikit-learn>=1.2.0,<2.0.0      # hive/data/door_client.py
pyyaml>=6.0,<7.0                # hive/main.py config loading
tqdm>=4.64.0,<5.0.0

# ── GPU acceleration (Apple silicon). Falls back to NumPy if absent ──────────
mlx>=0.10.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# ── Figures ──────────────────────────────────────────────────────────────────
matplotlib>=3.7.0,<4.0.0

# ── Vision pathway ───────────────────────────────────────────────────────────
pandas>=1.5.0,<3.0.0            # hive/vision/photoreceptor_database.py
opencv-python>=4.7.0,<5.0.0     # hive/vision/compound_eye.py, stimulus_generator.py

# ── Web UI backend (server.py + frontend/). Not needed for validation runs ───
fastapi>=0.100.0,<1.0.0
uvicorn>=0.22.0,<1.0.0
pydantic>=2.0.0,<3.0.0

# ── Optional ─────────────────────────────────────────────────────────────────
# cupy: CUDA backend in hive/engine/oscillator_gpu.py and coupling_gpu.py. Import is
# guarded, so it is not installed by default. On CUDA hardware:
#   pip install cupy-cuda12x

# ── Development ──────────────────────────────────────────────────────────────
pytest>=7.2.0,<9.0.0
